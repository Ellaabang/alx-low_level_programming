#include "main.h"

/**
 * main - return 1 if c is an uppercase alphabet
 *@c: the character to check
 * Return: 0 (success)
 */

 int main(void)
 {
        int i;

        for (i = 'A'; i <= 'Z'; i++)
        {
            if (i == c)
            {
                return (1);
            }
        }

        return (0);
 }
 